{% extends 'layout.html.twig' %}

{% block content %}
	<h1>Players</h1>
	<ul>
		{% for user in users %}
			<li>
				<a href="/items/show?id={{ user.id }}">
					{{ user.id }}
					{{ user.name }}

					{{ user.description }}
					{{ user.score }}</a>
			</li>
		{% else %}
			<li>Nothing to display</li>
		{% endfor %}
	</ul>
	<a href="/items/add">Ajouter</a>
{% endblock %}
